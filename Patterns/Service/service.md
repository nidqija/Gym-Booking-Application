# Service Pattern


### purpose
 The Service Pattern is a design pattern that promotes separation of concerns by encapsulating business logic and data access in separate service classes. This allows for better organization, maintainability, and testability of the code.
&nbsp;
### when to use
when we need to use the same business logic across multiple components, or when we want to separate business logic from presentation logic.
