services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app  # Maps your local folder to the container
    environment:
      - PYTHONUNBUFFERED=1
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload

